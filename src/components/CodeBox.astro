<astroscript>
export function CodeBox({ code }) {
  function copyToClipboard() {
    navigator.clipboard.writeText(code)
      .then(() => {
        alert("Command successfully copied!");
      })
      .catch(() => {
        alert("Cannot copy commands.");
      });
  }

  return (
    <div>
      <pre>
        <code
